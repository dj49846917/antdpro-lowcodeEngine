import Preview from "@/components/Preview";
import React, { useMemo } from "react";

const InlinePageConfig = () => {
  return <Preview pageId={"103996157045895168"} />
}

export default React.memo(InlinePageConfig)
